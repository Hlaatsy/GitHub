# Defaults applied to every post in this project's queue. A post's own front
# matter overrides anything here.
#
# IDENTICAL publishes through its own LinkedIn app and page -- see
# linkedin/README.md, "Three separate apps". Setting the profile once here
# means a new post cannot accidentally publish as KhutsoGRC by forgetting a
# line.
profile: identical
visibility: PUBLIC
